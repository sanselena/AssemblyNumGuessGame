# AssemblyNumGuessGame

The NumGuessGame file is an assembly code containing an input that asks player to pick a number between 0 and 9, and lets player know if their guess is lower, greater or equal to the secret number declared manually in the code. The GAME_LOOP structre lets the player keep guessing until they get the answer right!